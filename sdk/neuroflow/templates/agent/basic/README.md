# ${agent_name}

**模板**: Basic  
**描述**: ${description}

## 快速开始

```bash
# 安装依赖
pip install -r requirements.txt

# 设置 API Key
export OPENAI_API_KEY="your-api-key"

# 运行
python ${agent_name}.py
```

## 说明

这是最小化 Agent 模板，适合：
- ✅ 快速原型
- ✅ 简单对话场景
- ✅ 学习 NeuroFlow

如需更复杂功能（MCP、文件访问等），请使用 standard 或 advanced 模板。
